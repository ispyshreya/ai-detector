# Frontend (.env) — copy to .env. The frontend no longer holds any API secrets;
# it only needs the URL of the Veil backend, which holds the keys server-side.

VITE_VEIL_API_URL="http://127.0.0.1:8000"

# Detector and LLM secrets now live in backend/.env — see backend/.env.example.
